import { React, useState } from 'react'

function Chek({variant }) {
    return (
        <>
            <button className={`deafult-button ${variant} `}>Can You ME </button>
        </>
    )
}

export default Chek